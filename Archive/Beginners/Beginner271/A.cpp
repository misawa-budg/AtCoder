#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N; cin >> N;

    cout << hex << setfill('0') << setw(2) << uppercase << N << endl;

    return 0;
}
