#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    double A, B; cin >> A >> B;

    cout << fixed << setprecision(3) << B / A << endl;

    return 0;
}
